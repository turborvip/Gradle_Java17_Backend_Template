#application.yml
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: uat
---

spring:
  application:
    name: template
  main:
    banner-mode : off
    sources : com.turborvip.template
  output:
    ansi:
      enabled: ALWAYS